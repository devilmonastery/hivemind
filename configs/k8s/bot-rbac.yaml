# Service Account for Hivemind Bot
# Note: no special RBAC is required. Guild-sync coordination uses database
# leases via the server's gRPC API, not Kubernetes Lease objects.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: hivemind-bot
  namespace: default  # Change to your namespace
