# Environment variables template
# Copy this file to .env and fill in your actual values

# Database credentials
DB_PASSWORD=postgres

# JWT signing key (generate with: openssl rand -base64 32)
# IMPORTANT: Use different keys for dev/staging/prod
JWT_SIGNING_KEY=your_jwt_signing_key_here

# OAuth encryption key (generate with: openssl rand -base64 32)
# IMPORTANT: Use different keys for dev/staging/prod
OAUTH_ENCRYPTION_KEY=your_oauth_encryption_key_here

# Session secret for web UI (generate with: openssl rand -base64 32)
# IMPORTANT: Use different keys for dev/staging/prod
SESSION_SECRET=your_session_secret_here

# Discord OAuth credentials
# Get these from: https://discord.com/developers/applications
DISCORD_CLIENT_ID=your_client_id_here
DISCORD_CLIENT_SECRET=your_client_secret_here
DISCORD_BOT_TOKEN=your_discord_bot_token_here

# Bot service token for development (generate with: openssl rand -base64 32)
# This is a simple shared secret for dev - production uses database tokens via CLI
# IMPORTANT: Use different tokens for dev/staging/prod
DEV_BOT_TOKEN=your_dev_bot_token_here

# Optional: Remote BuildKit instance for faster Docker builds
# Uncomment and set to your BuildKit host to use remote building
# Run 'make buildx-remote-setup' after setting this to configure Docker buildx
# BUILDKIT_REMOTE_HOST=tcp://172.17.23.17:1234

# Optional: Docker Registry (default: devilmonastery)
# DOCKER_REGISTRY=your-registry

# Optional: Docker Version Tag (default: auto-generated from date)
# DOCKER_VERSION=2024.12.01
