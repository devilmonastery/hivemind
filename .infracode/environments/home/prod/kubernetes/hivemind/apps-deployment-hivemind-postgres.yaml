# Code generated by infracode. DO NOT EDIT.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: hivemind
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          # pgvector + pg_gembed (in-DB MiniLM embeddings), pinned by digest so nodes
          # always repull the exact build (tag reuse won't repull).
          image: registry.local.rothwell.us/devilmonastery/postgres-gembed:18.4-bookworm@sha256:eaa9bf8116b51eebce929960e70258d4edc399ee9a57c4fcd072fee42dba54c7
          ports:
            - containerPort: 5432
              name: postgres
          env:
            - name: POSTGRES_DB
              valueFrom:
                configMapKeyRef:
                  name: postgres-config
                  key: POSTGRES_DB
            - name: POSTGRES_USER
              valueFrom:
                configMapKeyRef:
                  name: postgres-config
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: db-password
          volumeMounts:
            - name: postgres-storage
              mountPath: /var/lib/postgresql
              subPath: pgdata
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              # Headroom for in-DB embedding inference (candle/MiniLM).
              memory: "2Gi"
              cpu: "1500m"
      volumes:
        - name: postgres-storage
          persistentVolumeClaim:
            claimName: postgres-pvc
