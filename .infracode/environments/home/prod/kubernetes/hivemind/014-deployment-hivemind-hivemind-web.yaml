# Code generated by infracode. DO NOT EDIT.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: hivemind-web
  namespace: hivemind
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: hivemind-web
  template:
    metadata:
      labels:
        app: hivemind-web
    spec:
      containers:
        - name: web
          image: registry.local.rothwell.us/devilmonastery/hivemind-web:2026.07.31.16.11
          ports:
            - containerPort: 8080
              name: http
            - containerPort: 9102
              name: metrics
          env:
            - name: SESSION_SECRET
              valueFrom:
                secretKeyRef:
                  name: hivemind-secrets
                  key: session-secret
          command:
            - /app/hivemind-web
            - --config
            - /etc/hivemind/config.yaml
          volumeMounts:
            - name: config
              mountPath: /etc/hivemind
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "200m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
      volumes:
        - name: config
          configMap:
            name: hivemind-web-config
