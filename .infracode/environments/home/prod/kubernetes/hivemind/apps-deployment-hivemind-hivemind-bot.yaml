# Code generated by infracode. DO NOT EDIT.
# Edit source manifests, run `make generate`, and commit the generated artifacts.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: hivemind-bot
  namespace: hivemind
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hivemind-bot
  template:
    metadata:
      labels:
        app: hivemind-bot
    spec:
      serviceAccountName: hivemind-bot
      containers:
        - name: bot
          image: registry.local.rothwell.us/devilmonastery/hivemind-bot
          ports:
            - containerPort: 9100
              name: metrics
          env:
            - name: DISCORD_BOT_TOKEN
              valueFrom:
                secretKeyRef:
                  name: hivemind-secrets
                  key: discord-bot-token
            - name: DISCORD_APPLICATION_ID
              valueFrom:
                secretKeyRef:
                  name: hivemind-secrets
                  key: discord-application-id
            - name: BACKEND_SERVICE_TOKEN
              valueFrom:
                secretKeyRef:
                  name: hivemind-secrets
                  key: backend-service-token
          command:
            - /app/hivemind-bot
            - run
            - --config
            - /etc/hivemind/config.yaml
          volumeMounts:
            - name: config
              mountPath: /etc/hivemind
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "200m"
      volumes:
        - name: config
          configMap:
            name: hivemind-bot-config
