# Code generated by infracode. DO NOT EDIT.
# Edit source manifests, run `make generate`, and commit the generated artifacts.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: hivemind-server
  namespace: hivemind
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hivemind-server
  template:
    metadata:
      labels:
        app: hivemind-server
    spec:
      containers:
        - name: server
          image: registry.local.rothwell.us/devilmonastery/hivemind-server
          ports:
            - containerPort: 4153
              name: grpc
            - containerPort: 9101
              name: metrics
          env:
            - name: JWT_SIGNING_KEY
              valueFrom:
                secretKeyRef:
                  name: hivemind-secrets
                  key: jwt-signing-key
            - name: OAUTH_ENCRYPTION_KEY
              valueFrom:
                secretKeyRef:
                  name: hivemind-secrets
                  key: oauth-encryption-key
            - name: DEV_BOT_TOKEN
              valueFrom:
                secretKeyRef:
                  name: hivemind-secrets
                  key: dev-bot-token
                  optional: true
            - name: DISCORD_APPLICATION_ID
              valueFrom:
                secretKeyRef:
                  name: hivemind-secrets
                  key: discord-application-id
            - name: DISCORD_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: hivemind-secrets
                  key: discord-client-secret
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: db-password
          command:
            - /app/hivemind-server
            - --config
            - /etc/hivemind/config.yaml
          volumeMounts:
            - name: config
              mountPath: /etc/hivemind
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          livenessProbe:
            tcpSocket:
              port: 4153
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            tcpSocket:
              port: 4153
            initialDelaySeconds: 5
            periodSeconds: 5
      volumes:
        - name: config
          configMap:
            name: hivemind-server-config
