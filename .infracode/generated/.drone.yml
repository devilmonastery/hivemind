# Code generated by infracode. DO NOT EDIT.
kind: pipeline
type: kubernetes
name: default
platform:
  os: linux
steps:
  - name: fmt
    image: golang:1.26.4
    commands:
      - printf 'machine github.com\nlogin oauth2\npassword %s\n' "$GITHUB_TOKEN" > ~/.netrc
      - chmod 600 ~/.netrc
      - git config --global url."https://oauth2:${GITHUB_TOKEN}@github.com/".insteadOf "https://github.com/"
      - go version
      - go fmt ./...
    environment:
      GITHUB_TOKEN:
        from_secret: github_module_token
      GONOSUMDB: github.com/devilmonastery/*
      GOPRIVATE: github.com/devilmonastery/*
    when:
      event:
        - push
        - pull_request
        - custom
      branch:
        - main
  - name: vet
    image: golang:1.26.4
    commands:
      - printf 'machine github.com\nlogin oauth2\npassword %s\n' "$GITHUB_TOKEN" > ~/.netrc
      - chmod 600 ~/.netrc
      - git config --global url."https://oauth2:${GITHUB_TOKEN}@github.com/".insteadOf "https://github.com/"
      - go vet ./...
    environment:
      GITHUB_TOKEN:
        from_secret: github_module_token
      GONOSUMDB: github.com/devilmonastery/*
      GOPRIVATE: github.com/devilmonastery/*
    when:
      event:
        - push
        - pull_request
        - custom
      branch:
        - main
  - name: test
    image: golang:1.26.4
    commands:
      - printf 'machine github.com\nlogin oauth2\npassword %s\n' "$GITHUB_TOKEN" > ~/.netrc
      - chmod 600 ~/.netrc
      - git config --global url."https://oauth2:${GITHUB_TOKEN}@github.com/".insteadOf "https://github.com/"
      - go test -race -coverprofile=coverage.txt -covermode=atomic ./...
    environment:
      GITHUB_TOKEN:
        from_secret: github_module_token
      GONOSUMDB: github.com/devilmonastery/*
      GOPRIVATE: github.com/devilmonastery/*
    when:
      event:
        - push
        - pull_request
        - custom
      branch:
        - main
  - name: build
    image: golang:1.26.4
    commands:
      - printf 'machine github.com\nlogin oauth2\npassword %s\n' "$GITHUB_TOKEN" > ~/.netrc
      - chmod 600 ~/.netrc
      - git config --global url."https://oauth2:${GITHUB_TOKEN}@github.com/".insteadOf "https://github.com/"
      - go build ./...
    environment:
      GITHUB_TOKEN:
        from_secret: github_module_token
      GONOSUMDB: github.com/devilmonastery/*
      GOPRIVATE: github.com/devilmonastery/*
    when:
      event:
        - push
        - pull_request
        - custom
      branch:
        - main
  - name: generate-verify
    image: golang:1.26.4
    commands:
      - printf 'machine github.com\nlogin oauth2\npassword %s\n' "$GITHUB_TOKEN" > ~/.netrc
      - chmod 600 ~/.netrc
      - git config --global url."https://oauth2:${GITHUB_TOKEN}@github.com/".insteadOf "https://github.com/"
      - make generate-verify
    environment:
      GITHUB_TOKEN:
        from_secret: github_module_token
      GONOSUMDB: github.com/devilmonastery/*
      GOPRIVATE: github.com/devilmonastery/*
    when:
      event:
        - push
        - pull_request
        - custom
      branch:
        - main
